experiment: mnist_linear
description: MNIST linear classifier (train + test accuracy)
data:
  name: MNIST
  source: torch
  config:
    train_size: 1000
    batch_size: 64
model:
  name: linear
algorithm:
  mode: train
  num_epochs: 2
  eval_period: 1
  lr: 0.1
system:
  device: cpu
