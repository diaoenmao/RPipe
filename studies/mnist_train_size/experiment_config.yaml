experiment: mnist_linear
description: MNIST linear classifier (real train + test accuracy)
data:
  name: MNIST
  config:
    train_size: 1000
    batch_size: 64
model:
  name: linear
algorithm:
  mode: train
  num_epochs: 1
  lr: 0.1
system:
  device: cpu
